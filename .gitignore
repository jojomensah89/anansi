node_modules/
.env
.env.local

# Your library. Raw payloads carry your session's view of your account —
# never commit them.
data/

# Fixtures ARE committed (they are the regression suite), but only after
# being scrubbed. See fixtures/README.md.
apps/web/.env.local

# turbo build cache
.turbo/

# Alchemy state, logs, and staged migration files are generated locally.
**/.alchemy/

# build outputs
dist/
apps/extension/.output/
apps/extension/.wxt/
*.tsbuildinfo

# Local review probes and generated architecture reports
outputs/

# test / coverage
coverage/
.nyc_output/

# seeded design canvas: 2.5MB of editor payload, regenerated from the
# .dc.html working files beside it
design/*.html
!design/*.dc.html

.superpowers/

/docs